# Scripts

Store reusable helper scripts for this workspace.

Guidelines:
- Keep scripts non-destructive by default.
- Add usage examples at top of each script.
- Put config in files, not hardcoded secrets.
